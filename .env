# ── Local development ────────────────────────────────────────────────────────
# This file is for running the admin panel on your laptop (npm start).
# For production (Vercel), set these in:
#   Vercel dashboard → Project → Settings → Environment Variables

REACT_APP_API_URL=http://localhost:5000